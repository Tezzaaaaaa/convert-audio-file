const input=document.querySelector('#file'),drop=document.querySelector('#drop'),queue=document.querySelector('#queue'),items=document.querySelector('#items'),clear=document.querySelector('#clear'),appearance=document.querySelector('#appearance'),convert=document.querySelector('#convert'),format=document.querySelector('#format');let files=[];const formats=['MP3','WAV','M4A','AAC','FLAC','OGG'];const size=n=>n<1048576?(n/1024).toFixed(1)+' KB':(n/1048576).toFixed(1)+' MB';function render(){queue.hidden=!files.length;convert.disabled=!files.length;items.innerHTML=files.map((f,i)=>`<div class="item"><div><div class="name">${escapeHtml(f.name)}</div><div class="meta">${size(f.size)} · Audio</div></div><select class="select" data-i="${i}" aria-label="Output format">${formats.map(x=>`<option ${x===format.value?'selected':''}>${x}</option>`).join('')}</select><button class="link remove" data-i="${i}">Remove</button></div>`).join('');document.querySelectorAll('.remove').forEach(b=>b.onclick=()=>{files.splice(+b.dataset.i,1);render()})}function escapeHtml(s){return s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function add(list){const incoming=[...list].filter(f=>f.type.startsWith('audio/')||/\.(mp3|wav|m4a|aac|flac|ogg)$/i.test(f.name));files.push(...incoming);render();if(incoming.length){drop.classList.add('has-file');document.querySelector('#sourceTitle').textContent=incoming.length===1?incoming[0].name:`${incoming.length} audio files`;document.querySelector('#sourceHint').textContent='Click to add more files'}}input.onchange=e=>add(e.target.files);['dragenter','dragover'].forEach(x=>drop.addEventListener(x,e=>{e.preventDefault();drop.classList.add('drag')}));['dragleave','drop'].forEach(x=>drop.addEventListener(x,e=>{e.preventDefault();drop.classList.remove('drag')}));drop.addEventListener('drop',e=>add(e.dataTransfer.files));clear.onclick=()=>{files=[];render();document.querySelector('#sourceTitle').textContent='Add audio file';document.querySelector('#sourceHint').textContent='Click to choose or drag and drop'};appearance.onclick=()=>{document.body.classList.toggle('dark');localStorage.setItem('dark',document.body.classList.contains('dark'))};if(localStorage.getItem('dark')==='true')document.body.classList.add('dark');convert.onclick=()=>{if(!files.length)return;convert.textContent='Ready';setTimeout(()=>convert.textContent='Convert',900)};
